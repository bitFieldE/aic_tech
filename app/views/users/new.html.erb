<% @page_title = "新規ユーザー登録" %>
  <h2 class="pb-4"><%= @page_title %></h2>

  <%= render "page/errors", object: @user %>

  <%= form_with(model: @user, url: users_path, local: true,
        class: "col-sm-8 col-md-6 col-lg-4 pt-4 resistration_form shadow") do |form| %>
    <%= render "page/user_form", form: form %>
  <% end %>
